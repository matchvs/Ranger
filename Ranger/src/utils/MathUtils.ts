
// 微信上的适配
// 其他端可以不用那么麻烦 showAll原有的尺寸正常缩放

class MathUtils {


    public static max(a: number, b: number) {

        return a > b ? a : b;
    }
    public static min(a: number, b: number) {
        return a < b ? a : b;
    }
}